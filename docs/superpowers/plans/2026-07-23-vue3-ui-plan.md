# Vue3 Element UI Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** Rebuild compound parser UI with Vue3 + Element Plus and FastAPI web server.

**Architecture:** FastAPI wraps existing Python core; Vue SPA consumes REST/SSE.

**Tech Stack:** Vue 3, Vite, Element Plus, FastAPI, uvicorn, smiles-drawer

## Global Constraints

- 测试不以 Electron 启动，用普通 Web 服务
- Tab 仅四项；设置默认收起为右侧 Drawer
- 结构化表按 Compound_ID 连表合并规则实现

---

### Task 1: Merge utility + tests

- [ ] Add `cdxml/text_ai/merge.py`
- [ ] Add `tests/test_text_ai_merge.py`
- [ ] Run pytest

### Task 2: FastAPI server

- [ ] Add `server/app.py` + requirements
- [ ] Parse / text-ai / config / export endpoints

### Task 3: Vue3 scaffold + UI

- [ ] Scaffold `web/` with Vite Vue Element Plus
- [ ] Implement layout matching mockups
- [ ] Wire APIs; start `uvicorn` + `vite` for verification
